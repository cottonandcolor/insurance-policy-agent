langgraph>=0.2.0
langchain-core>=0.3.0
chromadb>=0.5.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pytest>=8.0.0
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12
httpx>=0.27.0
pypdf>=5.0.0  # scripts/convert_public_policies.py — PDF → txt for public specimens

# Live mode uses direct Ollama/OpenAI HTTP calls (see src/llm/client.py).
# Optional: crewai>=0.86 requires Python 3.10+ if you prefer CrewAI workers.
